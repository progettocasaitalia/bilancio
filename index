<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ProgettoCasaItalia | Trasparenza Reale</title>
    <style>
        :root {
            --bg: #f0f2f5;
            --card: #ffffff;
            --primary: #55efc4; /* Verde Sanità */
            --text: #2d3436;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Navbar elegante */
        nav {
            width: 100%;
            padding: 20px 0;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(0,0,0,0.05);
            display: flex;
            justify-content: space-around;
            align-items: center;
        }

        .logo { font-weight: 800; letter-spacing: -1px; font-size: 1.4em; color: #1a1a1a; }
        .logo span { color: #00b894; }

        /* Contenitore Principale */
        .hero {
            max-width: 900px;
            width: 90%;
            text-align: center;
            padding: 60px 20px;
        }

        h1 { font-size: 2.8em; margin-bottom: 10px; letter-spacing: -1.5px; line-height: 1.1; }
        p.subtitle { font-size: 1.2em; color: #636e72; margin-bottom: 40px; }

        /* Il Grafico "Pseudo-3D" in puro CSS */
        .chart-container {
            position: relative;
            width: 320px;
            height: 320px;
            margin: 50px auto;
            perspective: 1000px;
        }

        .pie {
            width: 100%;
            height: 100%;
            border-radius: 50%;
            background: conic-gradient(
                #ff7675 0%
